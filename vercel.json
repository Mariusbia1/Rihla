{
  "buildCommand": "cd rihla-vente && npm install && npm run build",
  "outputDirectory": "rihla-vente/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
