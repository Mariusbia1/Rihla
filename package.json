{
  "name": "rihla",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix rihla-vente run dev",
    "build": "npm --prefix rihla-vente run build",
    "preview": "npm --prefix rihla-vente run preview"
  }
}
